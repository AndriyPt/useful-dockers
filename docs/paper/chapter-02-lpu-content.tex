% Постановка проблеми.
% Аналіз останніх досліджень та публікацій.
% Формулювання цілі статті.
% Виклад основного матеріалу.
% Висновки.
% Список літератури англійською мовою за стандартом APA Style (використання транслітерації при переклад


\begin{center}
    \textbf{Постановка проблеми}
\end{center}

  Сертифіковані медичні прилади зазвичай вимагають значних фінансових інвестицій. Кожен із ним має відповідний процес
  вишколювання персоналу. Проте, значну частину знань можна отримати без реального приладу, а у симуляції. Для цього
  будують симулятори органів людського тіла. Використання такого підходу здешевлює процес навчання і полегшує логістику
  вишколу. Звісно він не замінить реальні прилади, але суттєво полегшує вишколювання спеціалістів.
  
  Моделювання процесів теплообміну у тканинах людського тіла можна описати кількома моделями
  \cite{kutz-zhu-heat-transfer-biological-systems}:
  \begin{itemize}
    \item Рівняння Пеннеса\cite{thermal-process-tissue-pennes} -- у модельованих м'яких тканинах береться усереднений
          ефект від потоку крові через об'єм.
    \item Моделювання головних судин, які впливають на температурні процеси органу чи органів, які досліджуються.
  \end{itemize}
  Із часом з'являються досконаліші інструменти, які суттєво полегшують створення судинних моделей. Проте вони є
  складнішими, але дають точніші результати. Модель описана рівнянням Пеннеса є достатньо для багатьох симуляцій.
  
  У цій статті кома перед індексом означає похідну по змінній, яка відповідає цьому індексу, тобто ${f_{,j} =
  \partial}{f/\partial}x_{j}$, ${{f_{,ij} = \partial^{2}}{f/\partial}x_{i}\partial x_{j}}{}$, при цьому використовується
  німе сумування за індексами, які повторюються.
  
  Задачу теплообміну у людських тканинах у заданій області $\Omega$ можна сформулювати у вигляді:
  
  \begin{equation}
    \label{eqn:theory_thermo_pennes_eqn}
    \nabla \cdot \lambda_\theta \nabla \theta(x) + \omega \rho_b C_b (\theta_c - \theta(x)) + q_m  = 0, x \in \Omega
  \end{equation}
  
  \noindent де $\Gamma = \partial \Omega$ -- межа області $\Omega$, $\lambda_\theta$ -- коефіцієнт теплопровідності,
  $\theta$ -- шукана температура в області $\Omega$,
  $\rho_b$ -- густина крові, $\omega$ -- перфузія крові через орган, $C_b$ -- питома теплоємність крові,
  $\theta_c$ -- внутрішня температура тіла, $q_m$ -- метаболічне тепло у тканині. На межі задамо умови:
  
  \begin{equation}
    \label{eqn:theory_thermo_cond_dirichlet}
    \theta(x) = \theta_{\Gamma_D}(x), x \in \Gamma_D
  \end{equation}
  
  \begin{equation}
    \label{eqn:theory_thermo_cond_neumann}
    q(x) = q_{\Gamma_N}(x), x \in \Gamma_N
  \end{equation}
  
  \begin{equation}
    \label{eqn:theory_thermo_cond_robin}
    -\lambda_\theta q(x) = h(\theta(x) - \theta_e), x \in \Gamma_R,
  \end{equation}
  
  \noindent де $q(x) = \frac{\partial \theta(x)}{\partial n}$ -- тепловий потік, $n_i$ -- компоненти вектора зовнішньої
  нормалі до межі $\Gamma$, $\theta_e$ -- температура середовища, $h$ -- коефіцієнт тепловіддачі. Крайові умови
  (\ref{eqn:theory_thermo_cond_dirichlet}), (\ref{eqn:theory_thermo_cond_neumann}) і (\ref{eqn:theory_thermo_cond_robin})
  -- це умови Діріхле, Неймана і Робіна відповідно. Умова Робіна описує тепловий обмін тіла із навколишнім середовищем.

  \begin{center}
    \textbf{Аналіз останніх досліджень та публікацій}
  \end{center}

Рівняння Пеннеса було сформульовано у 1948 році у статті \cite{thermal-process-tissue-pennes}. У честь п'ятдесятиріччя
оригінальної статті її параметри були уточнені і детальніше викладені \cite{thermal-process-tissue-pennes-explained}. На
даний момент у багатьох дослідженнях для опису моделі процесів всередині людського тіла використовують це рівняння.
Зокрема у працях \cite{example-pennes-heterogeneous-blood-perfusion},
\cite{example-pennes-instantaneous-moving-heat-source}, \cite{example-pennes-nonlinear-specific-heat},
\cite{example-pennes-radiofrequency-tumor-ablation}.

Для отримання числових розв'язків цього рівняння використовується МСЕ і МГЕ. У статті
\cite{example-pennes-bem-heterogeneous-tissue} розглядається неоднорідні властивості тканин людського тіла і
застосовується МГЕ для отримання розв'язків. Фундаментальна стаття \cite{example-pennes-bem-transfer-equation} описує
принцип використання МГЕ для рівняння Пеннеса.

%   In this paper, the boundary element method (BEM) approach is applied to solve the Pennes (1948) bioheat equation. The
%   objective is to develop the BEM formulation and demonstrate its feasibility. The basic BEM formulations for the
%   transient and steady-state cases are first presented. To demonstrate the usefulness of the BEM approach, numerical
%   solutions for 2-D steady-state problems are obtained and compared to analytical solutions. Further, the BEM
%   formulation is applied to model a conjugate problem for an artery imbedded in a perfused heated tissue. Analytical
%   solution is possible when the conduction in the x-direction is negligible. The BEM and analytical results have very
%   good agreement.

%   Boundary element method with bioheat equation for skin burn injury
%   https://doi.org/10.1016/j.burns.2009.01.010

% Mathematical modeling has proven to be a reliable approach for investigating the temperature distribution inside
% biological systems. One of the most commonly used mathematical models for describing the heat transfer inside biological
% tissues is the Pennes bioheat equation [3]. The Pennes bioheat equation or the bioheat equation in short, is a
% practically simple model that distinguishes itself from the classical heat diffusion equation by including the thermal
% effects due to blood perfusion and metabolic heat generation, i.e. features that are identified in biological tissues.
% It has since become the starting point of scientific modeling work in bioheat transfer. Although various improved and
% alternative models have emerged, the Pennes bioheat model is still widely in use due to its accuracy and its simplicity
% in numerical coding. The use of mathematical models based on the Pennes bioheat equation to predict the temperature
% changes inside the human skin when subjected to heat has been a very popular method.

% Among the more notable works are
% those by Diller et al. [4] and Ng and Chua [5], [6], [7]. 

% Solutions of these models were obtained using numerical
% techniques such as the finite difference method and the finite element method. The boundary element method is a
% relatively new numerical technique when compared with the finite element method and the finite difference method. While
% less popular than the latter two, the boundary element method has been gaining many of the researchers’ attention of
% late. Unlike the finite element method and the finite difference method, the boundary element method is classified as a
% boundary method, meaning that only the boundary of the solution domain has to be discretized as part of the numerical
% scheme [10]. This offers a numerical alternative that is computationally less expensive than the other numerical methods
% that may be classified as domain methods (full domain discretion is required). The boundary element method has been used
% for solving various problems pertaining to the heat transfer inside biological tissues [8], [9]. It may be interesting
% to examine the feasibility of this method in simulating the temperature changes inside the human skin during burns. In
% this study, the boundary element method was used to obtain a numerical solution of the temperature distribution inside
% the human skin when subjected to a constant temperature heat source. Ideally, by accurately predicting the temperature
% within the skin tissues using proper computer-programmed codes, we may predict the seriousness of different burns
% effectively. Nonetheless, in this preliminary study, we shall only consider the steady state temperature changes inside
% the human skin.

% H.H. Pennes
% Analysis of tissue and arterial blood temperatures in the resting forearm
% J Appl Physiol
% (1948)
% K.R. Diller et al.
% A mathematical model for the thermal efficacy of cooling therapy for burns
% J Burn Care Rehabil
% (1983)
% E.Y.K. Ng et al.
% Prediction of skin burn injury. Part 1. Numerical modeling
% Proc Inst Mech Eng Part H: J Eng Med
% (2002)
% E.Y.K. Ng et al.
% Mesh independent prediction of skin burns injury
% J Med Eng Technol
% (2000)
% E.Y.K. Ng et al.
% Quick numerical assessment of skin burn injury with spreadsheet in PC
% Int J Mech Med Biol
% (2001)
  
  \begin{center}
    \textbf{Формулювання цілі статті}
  \end{center}
  
  Для розв'язування задач типу (\ref{eqn:theory_thermo_pennes_eqn}) (\ref{eqn:theory_thermo_cond_dirichlet})
  (\ref{eqn:theory_thermo_cond_neumann}) (\ref{eqn:theory_thermo_cond_robin}) використаємо метод приграничних елементів
  (МПГЕ)\cite{grytsko-cobem}, який належить до сімейства методів граничних елементів (МГЕ)\cite{brebbia-bem}. МГЕ мають
  кілька переваг у порівнянні із методом скінчених елементів (МСЕ)\cite{bahvalov-et-al}, зокрема:
  \begin{itemize}
    \item Розбиття границі області на дискретні елементи, а не цілої область.
    \item Зменшує розмірність задачі і відповідної кількості лінійних алгебраїчних рівнянь.
    \item Висока точність розв'язку на границі області.
  \end{itemize}
  Такі переваги роблять МГЕ привабливими для використання у симуляторах.
  
  МПГЕ дискретизує не саму границю, а приграничну зону. Це дозволяє ввести додатковий ступінь вільності, а саме ширину
  цієї зони. Він дозволяє покращувати точність стаціонарних задач. Також інтеграли, які обчислюються мають сингулярність
  на межі, а не всередині області інтегрування. Це дозволяє використовувати звичайні формули інтегрування для обчислення
  розв'язку. Симулятори можуть базуватися на МПГЕ.
  
  Візьмемо фундаментальні розв'язки рівняння Пеннеса (\ref{eqn:theory_thermo_pennes_eqn}) із
  \cite{pennes-equation-bem-fundamental-solutions} і позначимо їх $\hat{\theta}$ і $\hat{q}$. Розглянемо таку область $B
    \subset R^n$, що $\Omega \subset B$, $\partial\Omega \cap \partial B = \varnothing$. Позначимо приграничну область $G =
    B \setminus \Omega$. Інтегральне подання розв'язку для МПГЕ запишемо у вигляді:
  
  \begin{equation}
    \label{eqn:thermo_cobem_pennes_theta}
    \theta(\xi) = \int \limits_G g(x)\hat{\theta}(x,\xi)dG(x)
  \end{equation}
  
  \begin{equation}
    \label{eqn:thermo_cobem_pennes_q}
    q(\xi) = \int \limits_G g(x)\hat{q}(x,\xi)dG(x)
  \end{equation}
  
  \begin{center}
    \textbf{Виклад основного матеріалу}
  \end{center}
  
  Область $G$ розіб'ємо на приграничні елементи $G_j$, $j=\overline{1,n}$. Кожен із цих елементів містить кусок границі
  області $\Omega$, $G_j \cap \Gamma \neq \varnothing$. Визначимо область $G$, яка містить область $\Omega$, причому
  $\partial G \cap \partial \Omega = \varnothing$.
  
  Дискретизуємо інтегральне подання \ref{eqn:thermo_cobem_pennes_theta}:
  
  \begin{equation}
    \label{eqn:thermo_cobem_pennes_theta_discrete}
    \theta(\xi) = g_j \int \limits_{G_j} \hat{\theta}(x,\xi)dG_j(x),
  \end{equation}
  
  \noindent де $g_j$ -- стале значення функції $g(x)$ на усьому пригранич­ному елементі $G_j$. Відповідно у точці
  колокації $x_i$ отримаємо:
  
  \begin{equation*}
    \theta(x_i) = g_j \int \limits_{G_j} \hat{\theta}(x,x_i)dG_j(x)
  \end{equation*}
  
  \noindent Таке рівняння можна переписати у вигляді $\theta_i = \theta_{\Gamma_D}(x_i) = H_{ij} g_j$, де: 
  
  \begin{equation*}
    H_{ij} = \int \limits_{G_j} \hat{\theta}(x,x_i)dG_j(x)
  \end{equation*}
  
  \noindent Відповідно, $q_i = q_{\Gamma_N}(x_i) = P_{ij} g_j$ -- це дискретне представлення для інтегрального подання
  (\ref{eqn:thermo_cobem_pennes_q}) і умови (\ref{eqn:theory_thermo_cond_neumann}), де:
  
  \begin{equation*}
    P_{ij} = \int \limits_{G_j} \hat{q}(x,x_i)dG_j(x)
  \end{equation*}
  
  Система лінійних алгебраїчних рівнянь (СЛАР) запишемо у вигляді:
  
  \begin{equation*}
    \begin{bmatrix}
      H_{11} & ... & H_{1k} & ... & H_{1l} & ...  & H_{1n} \\
      ... \\
      H_{k1} & ... & H_{kk} & ... & H_{kl} & ...  & H_{kn} \\
      ... \\
      P_{l1} & ... & P_{lk} & ... & P_{ll} & ...  & P_{ln} \\
      ... \\
      P_{n1} & ... & P_{nk} & ... & P_{nl} & ...  & P_{nn}
    \end{bmatrix}
    \begin{bmatrix}
      g_1 \\
      ...   \\
      g_k \\
      ... \\
      g_l \\
      ... \\
      g_n
    \end{bmatrix}
    =
    \begin{bmatrix}
      \theta_1 \\
      ...   \\
      \theta_k \\
      ... \\
      q_l \\
      ... \\
      q_n
    \end{bmatrix}
  \end{equation*}
  
  \noindent Розв'язок СЛАР підставимо у рівняння (\ref{eqn:thermo_cobem_pennes_theta_discrete}). Завдяки цьому ми зможемо
  обчислити температуру у довільній точці області $\Omega$.
  
  \begin{center}
    \textbf{Висновки}
  \end{center}
  
Для порівняння методів візьмемо область $\Omega$ у вигляді квадратної пластини, довжина сторони 1 сантиметр.
Розв'яжемо задачу (\ref{eqn:theory_thermo_pennes_eqn}) (\ref{eqn:theory_thermo_cond_dirichlet}). Візьмемо наступні
значення коефіцієнтів: $\lambda_{\theta} = 1 \frac{\text{Вт}}{\text{м}\cdot\text{К}}$, $\omega = 1 \frac{1}{c}$, $\rho_b
= 1 \frac{\text{кг}}{\text{м}^3}$, $C_b = 2 \frac{\text{Дж}}{\text{кг}\cdot\text{К}}$, $\theta_c = {0}^\circ C$ і $q_m =
0 \frac{\text{Вт}}{\text{м}^3}$. На границі області $\Omega$ задамо значення відомого аналітичного розв'язку 
$\theta(x) = sinh(x_2)$.

Порівняємо результати отримані МСЕ і МПГЕ. У першому чисельному експерименті було використано 56 скінчених елементів і
40 приграничних елементів. Максимальна похибка у МСЕ приблизно рівна 0,09, а в МПГЕ 0,06. У другому експерименті було
226 скінчених і 120 приграничних елементів. У МСЕ максимальна похибка приблизно рівна 0,09, а в МПГЕ -- 0,00025. В обох
чисельних експериментах ширина приграничної смуги 0,4.

Вищенаведені результати показують, що МПГЕ застосовний до розв'язування задачі (\ref{eqn:theory_thermo_pennes_eqn})
(\ref{eqn:theory_thermo_cond_dirichlet}). Також, вимагає меншої кількості елементів дискредитації для отримання високої
точності у порівнянні із МСЕ.
